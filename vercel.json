{
    "version": 2,
    "functions": {
        "src/server.ts": {
            "runtime": "nodejs20.x",
            "includeFiles": "dist/**"
        }
    },
    "installCommand": "npm install",
    "buildCommand": "npm run build",
    "routes": [{ "src": "/(.*)", "dest": "src/server.ts" }]
}
