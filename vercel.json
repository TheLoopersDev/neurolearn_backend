{
    "version": 2,
    "functions": {
        "src/server.ts": {
            "includeFiles": "dist/**"
        }
    },
    "installCommand": "npm install",
    "buildCommand": "npm run build",
    "routes": [{ "src": "/(.*)", "dest": "src/server.ts" }]
}
